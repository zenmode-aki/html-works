# 🧭 作業前に、必ずここを読む

このファイルは**地図**です。中身は他のファイルにあります。ここは短く保ちます。

## 🔤 英訳は「直訳」。こなれさせない

普通の翻訳指示とは**逆**です。

- 日本語の1文 = 英語の1文。結合・分割・順番の入れ替えをしない
- 語彙は中学生レベル（英検3級程度）。Google翻訳レベルでいい
- ❌ 意味は同じだが、言い方がうまくなっている英語
- ✅ 同じことを、同じ順番で言っているだけの英語

→ 詳しくは `PROMPT.md` の「5️⃣ 翻訳は直訳」の節

## 📁 記事の置き場所

- **シリーズ**（何本も増えるもの）→ トップレベルに専用フォルダ
  （`旅/` `バギオ/` `マレーシア/` `気づきと学び/` `ありがとうを言いたいもの/` …）
  中身は `情報源.txt` と `記事/`
- **単発**（一本もの）→ `あとで記事にするもの/`
- **公開した記事** → `works/<slug>/`（`./new-post.sh <slug>` で作る）

## 🌐 表示言語（日本語など）

- 公開記事は**英語で書く**。日本語・韓国語などの訳は必ず公開中の英語本文から直接作り、`works/<slug>/i18n/<lang>.json` に「英文 → 訳」で置いて `python3 tools/i18n.py` で埋め込む。日本語訳から韓国語などへ二次翻訳しない（2026-09-24 本人の指定）
- 新しい記事を公開したら、**訳もつけてから push**（`python3 tools/i18n.py --todo <slug>` で訳すべき文が出る）
- 日本語訳も「こなれさせない」。ただし日本語として自然に読めること。元の日本語（`source.md`）は意味の確認に使ってよいが、翻訳の原文は英語にする
- 韓国語訳は運営者本人が読めず正しさを確認できない。その注意書きは韓国語表示のときだけUIに出す
- **日本語の一人称は「私」**（「僕」は使わない。「僕たち」「僕ら」も「私たち」）。歌詞などの引用は原文のまま（2026-09-24 本人が決めた）

## 🙋 一人称は、どこでも「私」（2026-09-24 本人が決めた・全体のルール）

- 訳だけでなく、**日本語の原稿・添削・下書き・source.md・ドキュメント、ぜんぶ「私」**で書く
- 本人の原稿や音声入力に「僕」が入っていたら、**「私」に直してから**使う（「僕たち」「僕ら」→「私たち」）
- 例外は歌詞などの**他人の作品の引用**だけ（原文のまま）
- 2026-09-24 に、リポジトリ内の「僕」を全部「私」に置き換えた（94ファイル）

→ 詳しくは `README.md` の「🌐 表示言語の切り替え」

## 🗣 記事の日本語は標準語（2026-09-24 本人が決めた）

- 本人いわく「関西弁だと調子に乗っている感じがする」。**記事に出る日本語は、全部標準語**にする（会話の引用・人のセリフも含む）
- 原稿や音声入力に関西弁（〜やん／ええ／〜やろ／わからん／いらん／〜やと思う など）があったら、**公開するときに標準語に直す**
- 2026-09-24 に全記事の日本語訳と原稿をチェックして、関西弁を標準語に置き換えた（10か所）
- 「めっちゃ」は全国で使う話し言葉なので、そのまま

## 🌏 表示言語（2026-09-25 更新）

- 公開記事の基準言語は英語。すべての表示訳は公開中の英語本文から直接作る。日本語などの訳を別言語の翻訳元にしない
- 現在40言語：`en ja ko zh zh-Hant es fr de pt id vi fil ms ru tr nl it pl sv nb da fi cs ro el uk sw ceb hi th bn ta te mr pa ne ar ur fa he`。フェーズB・C・Dまで全177記事に対応済み。歌詞の引用2文はタミル語・テルグ語でも原文のまま残す。右から書くアラビア語・ウルドゥー語・ペルシャ語・ヘブライ語はRTL表示に対応
- 新しい未確認訳には `unverified: true` とその言語の注意書きを設定する
- メニューは現在の地球儀・小旗デザインを維持し、検索欄は置かない。英語名頭文字 A〜Z ジャンプを保つ
- 中国語は簡体字の訳と既存の繁体字対応を維持する
- 足し方：`i18n/ui.<lang>.json`・`i18n/top.<lang>.json`・`works/*/i18n/<lang>.json` を置いて `python3 tools/i18n.py`。`python3 tools/i18n.py --check` と `python3 tools/check.py --site` を確認する

## ✂️ 訳の分担：Claude は5言語だけ、残りは Codex（2026-09-25 本人が決めた）

- 言語を増やしたので、1文直すだけで40言語ぶんの訳が要る。**Claude（Opus）のトークンを翻訳に使いすぎない**ための分担
- **Claude がやる**：記事を書く・直すときは **英語・日本語・韓国語・中国語（簡体）・繁體（簡体から OpenCC で自動）** の5つだけ訳して、そのまま push してよい
- **残りの言語はやらなくていい**。訳のない文は、その言語のページでは**自動で英語のまま**表示される（壊れない）
- **Codex（ChatGPT）がやる**：あとでまとめて、訳が抜けている文を埋める
  1. `python3 tools/i18n.py --check` で、どの記事のどの言語が抜けているか見る
  2. `python3 tools/i18n.py --todo <slug> <lang>` で抜けている英文を出して、`works/<slug>/i18n/<lang>.json` に足す（直訳・1文＝1文）
  3. `python3 tools/i18n.py` → `python3 tools/check.py --site` → 自分のファイルだけ名前で `git add` して push
- 本人が Codex に頼むときの一言：**「`python3 tools/i18n.py --check` で抜けている訳を全部埋めて、`python3 tools/i18n-audit.py --all-langs` で確かめてから push して」**
- **訳は「ファイルがあるか」だけでなく「画面に出ているか」まで確かめる**（2026-09-25 本人の要望）。`--check` は訳のファイルの抜けしか見ないので、見出し・トップの一覧のラベルなどが英語のまま残っても気づけなかった
  - push の前に **`python3 tools/i18n-audit.py <slug> _top`**（本物のブラウザで ?lang=ja/ko/zh/zh-Hant を開いて、英語の文が残っていないか探す）。❌ が出たら直してから push
  - Codex は残りの言語を埋めたあと **`python3 tools/i18n-audit.py --all-langs`** で確かめる
  - トップの一覧に出る記事の短いラベル（meta.json の `label`）の訳は、`works/<slug>/i18n/<lang>.json` の **`"label"`** に書く。抜けは `--check` / `--todo-top` にも出る

## 🌙 ダークモード

- `tools/i18n_runtime.js` が全ページに入れている。**いつもライトで見せる**（2026-09-26 本人：「ライトのほうが見栄えがいいから基本そっちで見せたい。ダークは見つけづらいところでいい」。端末の設定には合わせない）
- 切り替えボタンは**ページのいちばん下に小さく**（「🌙 ダークモード」）。上のバーには置かない。選んだら localStorage の `pengesso-theme-v2` に覚える（名前を v2 に変えて、前にダークを選んだ人も一度ライトに戻した）
- 同じファイルに「読みにくい文字の見張り番」がある：ダークでは全部の文字、ライトでは小さなラベル類だけ、背景との差が小さい文字を色味を残して明るく／暗くする
- 新しい部品を作るときは、白い背景（#fff）を直書きしたら `html[data-theme="dark"]` のときの色も付ける

## ⏮ 前の記事へ（2026-09-25）

- **「次の記事へ」は手で決めない**（2026-09-25：手で決めていたらループ・行き止まりができた）。`python3 tools/next-links.py` が全記事を1本の道に並べる。章ごと（旅・海外は国ごと、フィリピンはバギオ→クラーク→セブ／名古屋の記事は話題ごと）に並べ、章の中は原稿フォルダの番号順（`マレーシア/記事/01_…`）→書いた順。シリーズは Part 1→2→3 でまとめる。「次の記事で書きます」のように順番を決めたいときは、meta.json に `"follows": "<前の記事のslug>"` を書く。最後はトップへ
- 章が変わるところの「次へ」は「NEXT · NEW TOPIC 🔀」（ここから話が変わります）。暮らしの記事はトップの部屋ごと、章は海外 → 国内の旅 → そのほかの順。名古屋の街の話のように章を決め打ちしたいときは meta.json に `"chapter"`
- 「前の記事」ボタンは `python3 tools/prev-links.py` が入れる（その記事を「次」に指している記事）
- **記事を公開したら、`tools/next-links.py` → `tools/prev-links.py` → `tools/i18n.py` の順に走らせる**。`check.py --site` がループ・はぐれ記事を見張っている

## 🔗 リンクを貼ったときのカード（OGP・2026-09-26）

- LINE・X などにリンクを貼ると、表紙の絵・タイトル・最初の一文がカードで出る。`tools/ogp.py` が `<head>` の目印（`<!-- ⬇️ OGP（tools/ogp.py） -->`）の中を作る。**手で書かない**
- `python3 tools/build-site.py` が毎回いっしょに走らせるので、新しい記事にも自動で入る。絵は `assets/thumbs-src/<slug>.jpg`

## 📍 写真の「撮った場所」は必ず消す（2026-09-26）

- スマホの写真には、撮った場所の緯度・経度（GPS）・日時・機種が入っている。2026-09-26 に、職場の近く・セブの部屋・クラークの寮の位置が入った写真が13枚、公開されていた
- 写真を足したら **`python3 tools/strip-exif.py`**（画質は変わらない。情報の部分だけ消す）。push のたびに GitHub の検査が `--check` で見張っている

## ⏱ 秒数は記事ごとに出す・いちばん下に「一覧に戻る」（2026-09-26 本人の要望）

- 題の上の札・「次の記事」の札・トップの一覧は、**その記事の本当の秒数**（本文の語数 ÷ 1分180語）。前は全部「15 SEC」と決め打ちだった。語数は右上の札とトップの一覧に出る
- 記事のいちばん下（次・前の記事のあと）に「📚 ← 一覧に戻る」。トップの一覧の、見ていたところに戻る
- どちらも `tools/post-bottom.py` が入れる。`tools/prev-links.py` が毎回最後に呼ぶので、いつもの手順（next-links → prev-links → i18n）で新しい記事にも入る。「次の記事」の秒数は `tools/next-links.py`
- 訳は、今ある「⚡ 15 SEC」の訳の数字の部分を入れ替える決まり（`i18n/ui.*.json` の patterns）で、40言語とも正しい秒数が出る

## 📸 写真の画質（2026-09-25）

- 本人の写真は `tools/embed.py` が **幅1400px・JPEG q84** で埋め込む（前は1200px・q72で、荒く見えていた）。1記事の上限は **1MB**
- 埋め込んだ写真を**もう一度縮めない**（圧縮を重ねるほど荒くなる）。縮めるなら必ず元の写真から作り直す
- 元の写真（記事フォルダの `*.jpeg`）は消さない。画質を上げ直すときに使う

## 🅱️ 細い文字は使わない（2026-09-24 本人の好み）

- 本人は**細い文字が全部嫌い**。本文も含めて全部太字でいいぐらい
- 新しいページ・部品・フォントを足すときも、細いウェイト（300〜400）や細身の書体を選ばない

## 📄 ローカルの原稿がいちばん新しい（2026-09-24 本人が決めた）

- `記事/` の中の原稿（日本語・英語）は、**本人がいちばん最近直した版**。本番の記事はこれに合わせる
- 公開・修正の前に、原稿と本番（`works/<slug>/source.md`・`i18n/ja.json`）が食い違っていないか確認する。違ったら**原稿のほうが正しい**
- 本人が「直したのに反映されてない」と言ったら、まず**エディタで保存されていない**可能性を伝える（ディスク上の内容しか読めないため）

### 🇯🇵 日本語と英語の原稿が両方あるときは、日本語が正（2026-09-24 本人が決めた）

- 本人が編集するのは**ほぼ日本語だけ**。英語で直すことはめったにない。英語版（`*_🇬🇧_*.txt`）は、日本語より**古いままのことが多い**
- なので、日本語と英語で中身が食い違っていたら、**日本語のほうが正しい**。英語版は信用しない
- 「本番にプッシュして」と言われたら：**① 日本語の原稿を読む → ② その日本語から英訳し直す（英語版の原稿ファイルも更新）→ ③ 記事を作って公開・push まで**やる


## 🔔 回答の最後に、残っているタスクを声かけする（2026-09-24 本人が決めた）

- 本人は「本番に出して」のあと、達成感で手が止まりやすい。脱線もしやすい
- **毎回の回答の最後に、[BACKLOG.md](BACKLOG.md) から「まだ残っていること」を2〜3個だけ書く**
- 終わったものは BACKLOG.md から消し、新しく出たものは足す。Codex 用の [AGENTS.md](AGENTS.md) にも同じことを書いた

## 🎨 HTMLでしかできないことを、どんどんやる（2026-09-24 本人が決めた）

- このブログの魅力は「AIが作るHTMLのブログ」であること。**普通のブログっぽい、文字だけのカードにしない**
- アニメーション・色分け・図・比べる表・動くUIなど、読みやすくなる工夫は**AIの判断で自由に入れてよい**（`prefers-reduced-motion` の逃げ道だけは必ず付ける）
- 長くて読みにくい段落は、**意味を変えずに**短く分ける・言い換える・見せ方を変えてよい（「AIが端折らない」より、読みやすさを優先する）

## ✍️ 添削と UI・UX は、聞かずにおすすめ案で反映する（2026-09-24 本人が決めた）

- 「添削して」と言われたら、**選択肢を並べて返事を待たない**。一番いい案で原稿ファイル（日本語＋英語版）に反映して push まで
- **UI・UX（デザイン・レイアウト・フォント・アイコン・使い勝手）も同じ**。AIの判断で実装して push まで
- 報告では「こう直した。違えば言って」と書くだけにする
- **削除だけは別**。ファイル・記事・カード・ブランチなどを消すのは、本人が明示的に「消して」と言ったときか、確認して OK をもらってから
- 質問していいのは、**削除・上書き／プライバシー（家族の情報など）／本人しか知らない事実**だけ。それも1回に1問まで

## 📝 記事ファイルの形

`記事/` の中は、日本語版と英語版を**対**で置きます。

```
01_少し寒いと、眠くなる.txt
01_🇬🇧_a-little-cold-makes-you-sleepy.txt
01_少し寒いと、眠くなる_1_写真の説明.jpg   ← 画像は同じ番号で並べる
```

## 🧭 何を書いて、何を書かないか

- 仕事 → 面白く、ポジティブに（不平・不満は書かない）
- 生活 → 逆。失敗・挫折・ネガティブを書く（幸せアピールはしない）
- 恋愛 → 書かない。ぺんげっそは恋愛しないキャラ。匂わせもしない
- お金があるアピール / 友達いるアピール → しない
- 苦手なもの・怖いもの → どんどん書く

→ 詳しくは `PLAN.md` の「🧭 投稿方針」

## ⚠️ このリポジトリは公開です

実名・宿の部屋番号・便名・会社名は書きません。

## 📚 詳しい話

| ファイル | 中身 |
|---|---|
| `PROMPT.md` | 記事を書くときの全ルール（英訳・トーン・HTML） |
| `README.md` | ブログ全体の決まりごと |
| `PLAN.md` | 何のために書いているか（方向） |
| `NEXT.md` | 作業の続き |
