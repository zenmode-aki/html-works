# 本番前環境（staging/）は git に乗せる。
# ChatGPT/Codex からも、スマホからも見られるようにするため。
# 公開はされるが、robots.txt と noindex で検索には出ない。

# 手元だけの試し置き場（必要なら使う）
scratch/

# 「どのSlackメッセージまで記事にしたか」の記録
.blog-queue/

.DS_Store

# Python の中間ファイル
__pycache__/
*.pyc
