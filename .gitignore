# 新UIの試作（2026-09-06〜）。あきくんが「本番にはまだ出さなくていい、
# ローカルでいい」と言っているので、うっかり push されないように外してある。
# 本採用が決まったら、この2行を消して git add する。
preview/

# 手元だけの試し置き場（必要なら使う）
scratch/

# 「どのSlackメッセージまで記事にしたか」の記録
.blog-queue/

# 毎朝の自動生成の下書き（本番に出すときだけ works/ に置く）
drafts/

.DS_Store

# Python の中間ファイル
__pycache__/
*.pyc
